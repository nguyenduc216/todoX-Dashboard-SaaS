@page "/rdance-fashion-demo"
@inject NavigationManager Navigation

<PageTitle>Video nhảy quảng cáo thời trang</PageTitle>

<MudStack Spacing="3">
    <MudProgressLinear Indeterminate="true" Color="Color.Warning" />
    <MudAlert Severity="Severity.Info">Đang chuyển tới trang video nhảy quảng cáo thời trang...</MudAlert>
</MudStack>

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo("/jobs/rdance/new", replace: true);
}
